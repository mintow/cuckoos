    <h1><%= link_to("#{@issue.tracker.name} ##{@issue.id}: #{@issue.subject}", @issue_url) %></h1>
    <%= render_email_issue_attributes(@issue, @author, true) %>
    <%= textilizable(@issue, :description, :only_path => false) %>
